apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todo-helm.fullname" . }}-test-connection"
  labels:
    {{- include "todo-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todo-helm.fullname" . }}:{{ .Values.service.port }}{{ .Values.prefix }}']
  restartPolicy: Never
