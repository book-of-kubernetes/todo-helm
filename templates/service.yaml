apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-helm.fullname" . }}
  labels:
    {{- include "todo-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-helm.selectorLabels" . | nindent 4 }}
